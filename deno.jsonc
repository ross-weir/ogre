{
  "fmt": {
    "files": {
      "exclude": [".git", "dist", "README.md"]
    }
  },
  "lint": {
    "files": {
      "exclude": [".git", "dist", "events/mod.ts"]
    }
  },
  "tasks": {
    "test": "deno test --unstable --coverage=./cov --allow-all",
    "cov": "deno --unstable coverage ./cov --lcov > coverage.lcov",
    "build:cli": "deno compile -A --output bin/ergode _build/native/cli.ts",
    "build:npm": "deno run -A _build/web/npm.ts",
    "build:web": "deno run -A _build/web/esbuild.ts"
  }
}
